# content/about/

Полный портретный лонгрид собран в `content/bio/bio-long.md` — это канонический текст
страницы «Обо мне» (источник для `site` и `github-pages`).

Если в будущем понадобится собирать страницу по секциям с разными `targets`
(например, кейсы — отдельно для Хабра), разнести bio-long.md на нумерованные файлы:
01-intro, 02-beginning, 03-dynasty, 04-path, 05-cases, 06-skills, 07-teaching,
08-univercon, 09-contacts — каждый с YAML-фронтматтером (title, targets, order).

Пока в этом нет необходимости: лонгрид цельный.
